# remember to run "sudo postmap /etc/postfix/transport" after making changes

lists.nix.is    mailman:
sys.velfag.is   mailman:


# This probably doesn't work, whoevers domain this is, you should remove
# this and add an entry to virtual redirecting mail to @local.nix.is
repnz.net	local:

