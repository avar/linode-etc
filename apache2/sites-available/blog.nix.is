<VirtualHost *:81>
    ServerName blog.nix.is
    DocumentRoot /var/www/blog.nix.is/

    # Close connection after 302 is sent
    KeepAlive Off

    # Redirect to the new site
    Redirect permanent / http://hinrik.net/
</VirtualHost>
