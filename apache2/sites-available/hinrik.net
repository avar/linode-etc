<VirtualHost *:81>
    ServerName hinrik.net
    ServerAlias www.hinrik.net

    # Close connection after 302 is sent
    KeepAlive Off

    # Redirect everything to the CPAN dist for now
    RewriteEngine On
    RewriteRule .* http://blog.nix.is [R=302,L,NE]
</VirtualHost>
