#!/bin/sh

perl -e 'exit 1 if shift >= 0.30' $(awk '{print $1}' /proc/loadavg);

if [ $? -eq 0 ] || [ -n "$FORCE" ]; then
    /usr/share/munin/munin-graph $@ || exit 1

    /usr/share/munin/munin-html $@ || exit 1
fi
